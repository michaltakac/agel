# Agel seL4 backend manifest

Generated by `./scripts/sel4-manifest.sh`. Everything below is read from
the artifacts that were actually built, not from documentation.

## Trusted base

These come from the pinned SDK release and from this repository, so they
are the same for everyone who builds this system. CI checks them.

| Artifact | SHA-256 |
|---|---|
| Microkit SDK version | 2.3.0 |
| board / configuration | qemu_virt_aarch64 / release |
| seL4 kernel (`sel4.elf`) | `57a02dea2c308eee6fd3ff0f975b63a973f93b6a71a742b42b441a41475441b3` |
| Microkit loader | `c4518e30a3b548ce92516b78e2631371648fc8f9bf04ce2192589b317e865f0e` |
| Microkit monitor | `10ab4477e52ea63c2bbc23e82c2df0f6717d1101ef6f17e42cbda74c54a43e47` |
| `libmicrokit.a` | `cc1cdd51dcb2a2007cb1d5d9b27e1f067503f038fc513bcd1d155cbd03ef4910` |
| System description | `e698bf9690675ce67dd799811e87b08755ca566e87503c0ddee4bf062a45470d` |

## This build

These depend on the compiler that produced them, so they identify one
build rather than the system. CI does not check them; reproducing them
requires the same toolchain.

| Artifact | SHA-256 |
|---|---|
| Rust | `rustc 1.98.1 (48a229cea 2026-09-01)` |
| Bootable image | `b5c53ffb45edf15381a4130b836defdaae7ccf01940f36bd46081776d2e5780c` |
| Protection domain `serial` | `473c75700d6ee9617fd850d749fd611e520ed57b50b8d044669fe6cc4806dc1d` |
| Protection domain `broker` | `d0c0a04b2cd34fa4223fe17bcc5655c753dbe73367baf633cbdbbfd21ac324e4` |
| Protection domain `recovery` | `91d01cb500487d689f7681b495cfbe68e8ddb8ef7c629d657bce57ef75001562` |
| Protection domain `world` | `fc6ecff4c9778c439504625d282aef5ae9ad19b7fc4fcff8d9468e450f409490` |

Protection domains are built for `aarch64-unknown-none-softfloat`. The seL4
kernel and `libmicrokit.a` are the published release binaries: nothing in
this repository compiles, patches, or configures the kernel.

## Kernel configuration

| Option | Set |
|---|---|
| `CONFIG_KERNEL_MCS` | yes |
| `CONFIG_ARM_HYPERVISOR_SUPPORT` | yes |
| `CONFIG_ENABLE_SMP_SUPPORT` | no |
| `CONFIG_DEBUG_BUILD` | no |
| `CONFIG_PRINTING` | no |

## What is and is not verified

The kernel binary above is an unmodified seL4 release. Agel adds no kernel
objects, no syscalls, and no patches; the Agel contract is answered by an
ordinary unprivileged server.

**This is not a verified configuration.** Every Microkit board ships an MCS
kernel, and seL4's verified-configurations table lists MCS proofs as ongoing
rather than complete on every architecture; this board additionally enables
hypervisor support, which is a further configuration outside the AArch64
proofs. Running on an unmodified verified *kernel implementation* is not
the same as running a *verified configuration*, and this spike does the
former.

Nothing here is a claim about the correctness of the Agel code above the
kernel. The system description grants the authority; the protection domains
are ordinary Rust; neither has been verified by anyone.
